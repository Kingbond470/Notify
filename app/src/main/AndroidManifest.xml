<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dev.kingbond.notify">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notify">
        <activity
            android:name=".ui.event.EventHomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.helpandsupport.HelpAndSupportActivity"
            android:theme="@style/Theme.SettingsAndHelp"
            android:exported="true" />
        <activity
            android:name=".ui.settings.SettingsUserProfileActivity"
            android:exported="true" />
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:theme="@style/Theme.SettingsAndHelp"
            android:exported="true" />
        <activity
            android:name=".ui.profile.EditProfileActivity"
            android:exported="true"
            android:theme="@style/Theme.EditProfile" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.event.MapsActivity"
            android:exported="true"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".ui.event.LocationSearchActivity"
            android:exported="true" />
        <activity
            android:name=".ui.event.EventActivity"
            android:exported="true" />
        <activity
            android:name=".ui.about.AboutActivity"
            android:exported="true" />
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:theme="@style/Theme.Goals"
            android:name=".ui.goal.GoalDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.task.TaskGoalActivity"
            android:exported="false"
            android:theme="@style/Theme.Goals" />
        <activity
            android:name=".ui.task.TaskActivity"
            android:exported="false"
            android:theme="@style/Theme.Goals" />
        <activity
            android:name=".ui.task.TaskHomeActivity"
            android:exported="true"
            android:theme="@style/Theme.Goals" />
        <activity
            android:name=".ui.goal.GoalHomeActivity"
            android:exported="true"
            android:theme="@style/Theme.Goals" />
        <activity
            android:name=".ui.splash.SpalshActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".ui.goal.GoalActivity"
            android:exported="true"
            android:theme="@style/Theme.Goals" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <receiver
            android:name=".alarmAndReminder.BootUpReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
        <receiver android:name=".alarmAndReminder.AlarmBrodcast" />
    </application>

</manifest>